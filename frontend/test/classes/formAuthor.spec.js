describe("form author", () => {
  describe("#constructor", () => {
    xit("Should construct with a form")
  });

  describe("#writeField", () => {
    xit("it should push a form field into the form's fields list")
  });

  describe("#writeFieldWithCheck", () => {
    xit("should push a field and a matching form field with similar names into the field's list")
  });
});
