describe("FormSubmission", () => {
  describe("#constructor", () => {
    xit("should instantiate with an instance of a form")
  });

  describe("#submissionReport", () => {
    xit("should return an obj containing field names pointing to field values")
  });
});
