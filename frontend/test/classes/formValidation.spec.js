describe("FormValidation", () => {
  describe("#constructor", () => {
    xit("should construct with a form")
  });

  describe("#errorsArePresent", () => {
    xit("it should return true when a form field does not pass it's error checks")
    xit("it should return false when all forms pass their error checks")
  });
});
